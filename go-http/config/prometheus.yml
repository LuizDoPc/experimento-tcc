global:
  scrape_interval: "5s"

scrape_configs:
  - job_name: 'go-http'
    metrics_path: /metrics
    static_configs:
      - targets: ['go-http:8080']
        labels:
          application: go-http